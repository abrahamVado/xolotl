# Android icon audit

//1.- Este informe resume el resultado del escaneo de iconos realizado con `tool/list_missing_icons.dart`.

//2.- El auditor revisa `android/app/src/main` y valida que cada referencia `@drawable` o `@mipmap` tenga un archivo.

//3.- Resultado actual: **no se detectaron iconos faltantes** en la estructura Android del proyecto.

//4.- Ejecuta `dart run tool/list_missing_icons.dart` para refrescar el listado cuando se modifique `res/` o `AndroidManifest.xml`.
